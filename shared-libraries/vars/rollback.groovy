def call(String releaseName = 'my-app', String namespace = 'default') {
    echo "Rolling back Helm release: ${releaseName} in namespace: ${namespace} (placeholder - Helm not yet configured on agent)"
}